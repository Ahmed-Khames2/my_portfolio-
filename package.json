{
  "name": "ahmed-khamis-dev",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "git clone https://github.com/flutter/flutter.git -b stable --depth 1 && ./flutter/bin/flutter build web --release"
  }
}
